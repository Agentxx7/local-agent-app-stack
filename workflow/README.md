# Workflow

The workflow is conversation-driven and operator-controlled. Cards translate a decision into a
bounded assignment; reports return evidence to the operator; only the operator closes the loop.
