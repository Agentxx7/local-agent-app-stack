# Rejected policy

`REJECTED` means physical absence from active code, configuration, tests, state, cache, assets,
fixtures, aliases, dispatch, and fallback paths. History and incident evidence are the archive.
Removal requires explicit scope, verification, and operator decision; a label alone is not removal.
