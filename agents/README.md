# Optional agents

Agents are operator-selected resources, not an automatic team. Add an agent definition only when
its responsibility, authority, inputs, outputs, limits, and escalation path are understood.
Use `agent-template.md`; never make an agent's existence imply that it must be invoked.
