# Dependency health

- Every dependency has an owner and purpose.
- Versions, provenance, licenses, and update policy are known.
- Runtime availability is distinguished from declared configuration.
- Missing dependencies fail visibly without silent substitution.
- Unused or duplicate dependencies are removed deliberately.
